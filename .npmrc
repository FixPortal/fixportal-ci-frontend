@fix-portal:registry=https://registry.npmjs.org

# Wait 7 days before resolving a newly published version. A freshly published
# package is the window in which a compromised maintainer account does its
# damage; `npm ci` installs from the lockfile and is unaffected. npm 11.10+.
min-release-age=7
